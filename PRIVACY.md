# 隐私说明

## 网页右侧快捷入口

v1.3.9 会在 HTTP / HTTPS 页面右侧显示一个本地 Buddy 拉手。它只根据网址、标题和 JobPosting 标记判断是否像岗位页，不读取正文；只有用户点击“识别这个岗位”后才会读取候选职位内容。

求职buddy采用本地优先设计。

## 数据存储

岗位、JD、简历、故事、复盘、Agent 记忆和设置默认保存在 Chrome 本地存储。项目没有自建远程数据库，也不会自动把内容发送给作者。

卸载扩展或清理浏览器数据可能导致内容丢失，请定期使用设置中的 JSON 导出功能备份。

## 页面读取

只有在用户点击“读取当前招聘页”后，扩展才会临时读取当前标签页中的职位结构化信息、标题、地点和职位描述。常驻的 Buddy 拉手脚本不读取页面正文。

## OCR

OCR 通过项目内置的 Tesseract.js、WebAssembly 核心和中文训练数据在本机运行。识别结果可能不完整，保存前需要人工核对。

## 在线 AI

用户可以在“设置 → AI 模型”自行连接 Kimi 或 DeepSeek。API Key 默认只保存在 `chrome.storage.session`，不会进入 JSON 备份、源码或 GitHub，关闭 Chrome 后失效。点击分析时，界面会再次说明即将发送的是当前 JD 或简历正文；取消后不会调用接口。

浏览器扩展中的会话存储仍不是操作系统级钥匙串。不要在公共设备上配置密钥；如需长期保存，应使用本地后端代理或系统钥匙串，而不是把密钥写进扩展源码。

## 公开仓库

请不要把真实简历、电话号码、证件号码、私人邮箱、面试记录、未公开业务数据或公司内部材料提交到公开仓库。
